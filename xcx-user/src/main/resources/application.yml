server:
  port: 8035
spring:
  application:
    name: xcx-user
  cloud:
    nacos:
      discovery:
        server-addr:
        username: nacos
        password: nacos
    loadbalancer:
      cache:
        enabled: false
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url:
    username: root
    password: root