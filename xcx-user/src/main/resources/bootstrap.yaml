spring:
  application:
    name: xcx-user
  cloud:
    nacos:
      server-addr:
      username: nacos
      password: nacos
      config:
        file-extension: yaml
        shared-configs:
          - dataId: shared-jdbc.yaml
          - dataId: shared-swagger.yml